{
  "name": "clawket-plugin",
  "private": true,
  "version": "2.3.9",
  "description": "Claude Code plugin shell for Clawket — hooks + prompts + skills; delegates runtime to @clawket/{cli,daemon,web}",
  "scripts": {
    "setup": "node scripts/setup.cjs"
  },
  "compat": {
    "@clawket/cli": ">=2.2.0 <3.0.0",
    "@clawket/daemon": ">=2.2.0 <3.0.0",
    "@clawket/web": ">=2.2.0 <3.0.0"
  }
}
