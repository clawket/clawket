{
  "name": "clawket-mono",
  "private": true,
  "scripts": {
    "lint": "cd web && npx eslint .",
    "typecheck": "cd web && npx tsc -b",
    "build": "cd web && npx vite build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "dependencies": {
    "@wireweave/markdown-plugin": "^1.2.10",
    "remark-gfm": "^4.0.1"
  }
}
